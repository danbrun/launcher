<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="AlwaysShowAction">

    <item
        android:id="@+id/profile_toggle"
        android:title="@null"
        app:showAsAction="always" />

    <item
        android:id="@+id/visibility_toggle"
        android:icon="@drawable/ic_baseline_visibility_24"
        android:title="@string/show_hidden"
        app:showAsAction="always" />

    <item
        android:icon="@drawable/baseline_push_pin_24"
        android:title="@string/add_item"
        app:showAsAction="always">

        <menu>

            <item
                android:id="@+id/pin_shortcut_button"
                android:icon="@drawable/baseline_shortcut_24"
                android:title="@string/pin_shortcut"
                app:showAsAction="always" />

            <item
                android:id="@+id/pin_widget_button"
                android:icon="@drawable/ic_baseline_widgets_24"
                android:title="@string/pin_widget"
                app:showAsAction="always" />

        </menu>

    </item>

</menu>
